---
import { type SectionProps, SectionBgType } from "@/types/eventTypes";
import Section from "./Section.astro";

type Props = SectionProps;

let { title, actions, bg, subtitle } = Astro.props;
if (bg === undefined) {
  bg = {
    sectionBgType: SectionBgType.heroSection,
  };
}
---

<Section
  className="h-[80vh]"
  title={title}
  actions={actions}
  bg={bg}
  subtitle={subtitle}
>
  <slot />
</Section>
